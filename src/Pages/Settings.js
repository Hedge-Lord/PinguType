import React from 'react';
import { useNavigate } from 'react-router-dom';

function Settings() {
    return(
        <body>
            <h1>Settings</h1>
        </body>
    );
}

export default Settings;